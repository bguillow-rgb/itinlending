{
  "name": "itinlending-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/gen-og.mjs",
    "build": "astro build",
    "postbuild": "node scripts/gen-trailing-slash-stubs.mjs dist && node scripts/check-links.mjs dist && node scripts/check-serp.mjs dist",
    "check:links": "node scripts/check-links.mjs dist",
    "check:serp": "node scripts/check-serp.mjs dist",
    "check:serp:report": "node scripts/check-serp.mjs dist --report",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/sitemap": "^3.2.0",
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource-variable/public-sans": "^5.2.7",
    "@fontsource-variable/source-serif-4": "^5.2.9",
    "astro": "^5.0.0",
    "sharp": "^0.34.0"
  }
}
